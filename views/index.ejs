<%- include('./partials/head') %>

<ul>
    <% data.forEach(person => {
        if(person.squad_id === 3 && person.role.includes("member")) { %>
          <li>

            <a href="www.google.com">
            <img src="<%= person.avatar %>" alt="profiel" id="profileImage" onerror="displayPlaceholder()">
                <%= person.name %> <%= person.prefix %> <%= person.surname %>
            </a>

            %>
          </li>
        <% }
      }) %>
</ul>

<%- include('./partials/foot') %>
