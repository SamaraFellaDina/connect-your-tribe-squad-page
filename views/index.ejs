<%- include('./partials/head') %>

<ul>
    <% data.forEach(person => {
        if(person.squad_id === 3 && person.role.includes("member")) { %>
          <li><a href="./person/<%= person.id %>"><%= person.name %></a>
            %>
          </li>
        <% }
      }) %>
    </ul>

<%- include('./partials/foot') %>
